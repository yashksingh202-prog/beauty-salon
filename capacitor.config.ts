import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.uday.beautysalon',
  appName: 'Beauty Salon',
  webDir: "dist/public"
};

export default config;
